package com.proutechos.sandbox.radiolivealacarte.server.service.recording;import org.quartz.Job;import org.quartz.JobDataMap;import org.quartz.JobExecutionContext;import org.quartz.JobExecutionException;import java.io.BufferedReader;import java.io.IOException;import java.io.InputStreamReader;public class StopRadioJob implements Job {    @Override    public void execute(JobExecutionContext context) throws JobExecutionException {        // Récupérer les données passées par le JobDataMap si nécessaire        JobDataMap dataMap = context.getMergedJobDataMap();        String url = dataMap.getString("url");        // Implémenter la logique pour arrêter l'enregistrement        System.out.println("Arrêt de l'enregistrement pour l'URL : " + url);        // Vous pouvez ajouter ici la logique pour vraiment arrêter le processus d'enregistrement    }}